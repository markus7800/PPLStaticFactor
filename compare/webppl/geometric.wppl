

var geometric = function(p) {
    var b = flip(p)
    if (b) {
        return geometric(p) + 1
    } else {
        return 0
    }
}

var model_rec = function() {
    var i = geometric(0.5)
    factor(Gaussian({mu: i, sigma: 1.}).score(5.))
}