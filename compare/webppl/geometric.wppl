

var geometric = function(p) {
    var b = flip(p)
    if (b) {
        return geometric(p) + 1
    } else {
        return 0
    }
}

var model_rec = function() {
    geometric(0.5)
}

var N = 1e5
console.log("N:", N*10)